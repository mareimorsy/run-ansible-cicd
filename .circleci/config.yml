version: 2.1

jobs:
  test:
    docker:
      - image: python:3.7-alpine3.11

    steps:
      - checkout
      - add_ssh_keys:
          fingerprints: ["54:03:09:2e:fd:b3:04:d1:99:84:99:a1:fd:4a:41:06"]
      - run:
          name: Install dependencies
          command: |
            # install the dependencies needed for your playbook
            apk --update add openssh-client bash
            apk add --update ansible 
      - run:
          name: Configure server
          command: |
            ls -l ~/.ssh
            # ssh-add ~/.ssh/id_rsa_5403092efdb304d1998499a1fd4a4106
            ansible-playbook -vvv -i inventory.txt main.yml --key=~/.ssh/id_rsa_5403092efdb304d1998499a1fd4a4106

workflows:
  welcome:
    jobs:
      - test
      # - anotherjob:
      #     requires: 
      #       - previousjob