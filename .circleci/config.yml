version: 2.1

jobs:
  server-configuration:
    docker:
      - image: mareimorsy/cicd-hamada:v11

    steps:
      - checkout
      - add_ssh_keys:
          fingerprints: ["54:03:09:2e:fd:b3:04:d1:99:84:99:a1:fd:4a:41:06"]
      # - run:
      #     name: Install dependencies
      #     command: |
      #       apk add --update openssh-client ansible 
      - run:
          name: Configure server
          command: |
            echo "[web]" > inventory.txt
            /usr/local/bin/aws ec2 describe-instances --region us-east-1 --query 'Reservations[*].Instances[*].PublicIpAddress' --output text >> inventory.txt
            ansible-playbook -i inventory.txt main.yml

  provsion-infrastructure:
    docker:
      - image: amazon/aws-cli

    steps:
      - checkout
      - run:
          name: Configure server
          command: |
            aws cloudformation deploy --template-file ec2.yml --stack-name web --region us-east-1

workflows:
  welcome:
    jobs:
      # - provsion-infrastructure
      - server-configuration
      # - anotherjob:
      #     requires: 
      #       - previousjob